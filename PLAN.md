# PLAN.md — 글로벌 마켓 브리핑 구현 계획서

> 구현 순서, 각 단계별 상세 작업 목록, 완료 기준 정의

---

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 글로벌 마켓 브리핑 (Global Market Briefing) |
| 한줄 설명 | 해외 법인 매출 데이터 시각화 + AI 주간 경영 브리핑 자동 생성 대시보드 |
| 목적 | 글로벌 제조업 AX(업무자동화) 직무 포트폴리오 |
| 기간 | 7~10일 |
| 기술 스택 | Next.js 14, TypeScript, Supabase, Claude API, Recharts, Tailwind CSS, Vercel |

---

## Phase 1: 프로젝트 기반 세팅 (Day 1)

### 1-1. Next.js 프로젝트 초기화

```bash
npx create-next-app@latest global-market-briefing --typescript --tailwind --eslint --app --src-dir
```

**설정:**
- App Router 사용
- src/ 디렉토리 구조
- TypeScript strict 모드
- import alias: @/ → src/

**패키지 설치:**
```bash
npm install @supabase/supabase-js @supabase/ssr
npm install recharts
npm install lucide-react
npm install framer-motion
npm install papaparse           # CSV 파싱
npm install @anthropic-ai/sdk   # Claude API (서버 사이드용)
npm install date-fns             # 날짜 유틸
npm install zustand              # 상태 관리
npm install react-dropzone       # 파일 드래그앤드롭
```

**완료 기준:**
- [ ] `npm run dev`로 localhost:3000 정상 작동
- [ ] Tailwind CSS 적용 확인
- [ ] TypeScript strict 모드 동작

---

### 1-2. Supabase 프로젝트 세팅

**⚠️ 기존 Supabase 인스턴스 공유 — 새 프로젝트 생성 X**

**Supabase 대시보드에서:**
1. 기존 Supabase 프로젝트에 `gmb_` 접두사 테이블 5개 생성 (users 제외 — auth.users 사용)
2. 각 테이블에 `gmb_` 접두사 RLS 정책 적용
3. Auth 설정: 이메일/비밀번호 인증 활성화 (이미 되어있으면 스킵)

**코드에서:**
```
src/lib/supabase/
├── client.ts      # 브라우저용 클라이언트
├── server.ts      # 서버 컴포넌트/API Route용 클라이언트
└── admin.ts       # Service Role 키 (관리자 작업용)
```

**환경 변수 (.env.local):**
```
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx
SUPABASE_SERVICE_ROLE_KEY=xxx
ANTHROPIC_API_KEY=xxx
```

**완료 기준:**
- [ ] Supabase `gmb_` 접두사 테이블 5개 생성 완료 (users 제외)
- [ ] RLS 정책 적용 완료
- [ ] 클라이언트에서 Supabase 연결 테스트 통과

---

### 1-3. 프로젝트 폴더 구조 생성

```
src/
├── app/
│   ├── page.tsx                          # 랜딩
│   ├── layout.tsx                        # 루트 레이아웃 (폰트, 메타)
│   ├── globals.css                       # 글로벌 스타일
│   ├── (auth)/
│   │   ├── login/page.tsx
│   │   └── signup/page.tsx
│   ├── (dashboard)/
│   │   ├── layout.tsx                    # 사이드바+헤더 레이아웃
│   │   ├── home/page.tsx
│   │   ├── sales/page.tsx
│   │   ├── sales/upload/page.tsx
│   │   ├── exchange/page.tsx
│   │   ├── briefing/page.tsx
│   │   ├── briefing/archive/page.tsx
│   │   ├── competitors/page.tsx
│   │   └── settings/page.tsx
│   └── api/
│       ├── briefing/generate/route.ts
│       ├── sales/upload/route.ts
│       ├── exchange/rate/route.ts
│       └── export/pdf/route.ts
├── components/
│   ├── shared/
│   ├── layout/
│   ├── home/
│   ├── sales/
│   ├── briefing/
│   ├── exchange/
│   ├── competitors/
│   └── landing/
├── hooks/
├── stores/
├── lib/
│   ├── supabase/
│   ├── utils/
│   └── constants/
├── types/
│   └── index.ts
└── middleware.ts
```

**완료 기준:**
- [ ] 모든 폴더와 빈 파일 생성 완료
- [ ] 각 page.tsx에 최소한의 placeholder 텍스트

---

## Phase 2: 인증 + 레이아웃 (Day 2)

### 2-1. Supabase 인증 (로그인/회원가입)

**구현:**
- 이메일 + 비밀번호 방식
- 로그인 페이지: 이메일, 비밀번호, 로그인 버튼, 회원가입 링크
- 회원가입 페이지: 이메일, 비밀번호, 이름, 회사명(선택), 가입 버튼
- 미들웨어: 비로그인 시 /login으로 리다이렉트

**UI 디자인:**
- 가운데 정렬 카드형 폼
- 심플하고 깔끔하게 (토스 로그인 참고)
- 로고 + 제목: "글로벌 마켓 브리핑"
- 에러 메시지: 인라인 표시 (빨간색)

**완료 기준:**
- [ ] 회원가입 → 이메일 확인 → 로그인 플로우 작동
- [ ] 비로그인 상태에서 대시보드 접근 시 /login 리다이렉트
- [ ] 로그인 후 /home으로 이동

---

### 2-2. 대시보드 레이아웃

**사이드바 (고정, 좌측 240px):**
```
🏠 홈
📊 매출 현황
💱 환율
🤖 AI 브리핑
🏢 경쟁사
⚙️ 설정
```

- 현재 페이지 하이라이트 (좌측 accent 바)
- 하단: 유저 이름 + 로그아웃
- 축소 모드: 아이콘만 표시 (선택사항)

**헤더 (상단 64px):**
- 좌측: 현재 페이지 제목
- 우측: 날짜 표시

**메인 영역:**
- max-width: 1200px
- padding: 24px
- 배경: #FAFAFA

**완료 기준:**
- [ ] 사이드바 네비게이션 작동 (모든 페이지 이동)
- [ ] 현재 페이지 하이라이트 표시
- [ ] 헤더에 페이지 제목 자동 변경
- [ ] 깔끔하고 프로페셔널한 느낌

---

## Phase 3: 핵심 기능 — 매출 데이터 (Day 3~4)

### 3-1. 설정 페이지: 국가(법인) 관리

**기능:**
- 국가 추가: 이름, 국가 코드, 통화 선택
- 국가 목록 표시
- 국가 삭제 (확인 모달)
- 기본 7개국 프리셋 버튼: "기본 설정으로 시작" 클릭 시
  - 미국(USD), 인도(INR), 폴란드(PLN), 중국(CNY), 일본(JPY), 독일(EUR), 베트남(VND) 자동 생성

**완료 기준:**
- [ ] 국가 CRUD 작동
- [ ] 프리셋 7개국 한번에 생성 가능
- [ ] DB에 정상 저장/조회

---

### 3-2. 매출 데이터 입력 (수동)

**기능:**
- 국가 선택 → 연도/월 선택 → 매출(현지통화), 주문건수, 신규고객, 메모 입력
- 이미 데이터가 있는 월은 "수정" 모드
- 저장 시 환율 자동 적용하여 revenue_krw 계산

**UI:**
- 심플한 폼 (위에 국가 탭, 아래 입력 필드)
- 저장 성공 시 토스트 알림

**완료 기준:**
- [ ] 매출 데이터 입력 → DB 저장 성공
- [ ] 기존 데이터 수정 가능
- [ ] 여러 국가/월 데이터 입력 가능

---

### 3-3. 매출 현황 대시보드

**상단 KPI 카드 4개:**
```
| 총 매출 (원화)      | 전월 대비         | 최고 성장 국가    | 최저 성장 국가     |
| ₩12,340,000,000   | ▲ +8.3%          | 인도 ▲+23%       | 폴란드 ▼-5%       |
```

**차트 영역:**
- **메인 차트: 월별 매출 추이** (Recharts AreaChart 또는 BarChart)
  - X축: 월 (최근 12개월)
  - Y축: 원화 환산 매출
  - 국가별 색상 구분 (스택형 또는 라인)
  - 호버 시 툴팁: 국가명, 금액, 전월대비

- **서브 차트: 국가별 비중** (Recharts PieChart 또는 Donut)
  - 국가별 매출 비중 %
  - 클릭 시 해당 국가 상세

**테이블 영역:**
- 국가 × 월 매트릭스 테이블
- 각 셀: 매출 금액 + 전월대비 색상
- 정렬 가능 (매출순, 성장률순)
- 행 호버 시 하이라이트

**필터:**
- 기간 선택: 최근 6개월 / 12개월 / 직접 선택
- 통화 단위: 원화(KRW) / 현지통화 토글

**완료 기준:**
- [ ] KPI 카드 4개 정상 표시 (전월대비 증감 포함)
- [ ] 월별 매출 추이 차트 렌더링
- [ ] 국가별 비중 차트 렌더링
- [ ] 매트릭스 테이블 정상 표시
- [ ] 필터 (기간, 통화) 작동
- [ ] 데이터 없을 때 EmptyState 표시

---

### 3-4. CSV 업로드

**업로드 플로우:**
1. 파일 드래그앤드롭 (react-dropzone)
2. papaparse로 CSV 파싱
3. 미리보기 테이블 (첫 5행) — "이 데이터가 맞나요?"
4. 컬럼 자동 매핑 (국가, 연도, 월, 매출, 주문건수, 신규고객, 메모)
   - 한글/영문 헤더 모두 지원
5. "업로드" 버튼 → API Route에서 DB 저장
6. 결과: "7개국 × 12개월 = 84건 저장 완료" 표시

**에러 처리:**
- 필수 컬럼 누락 시 에러 표시 ("'매출' 컬럼을 찾을 수 없습니다")
- 중복 데이터 시 덮어쓰기 확인 모달
- 숫자 형식 오류 시 해당 행 하이라이트

**샘플 CSV 다운로드:**
- "샘플 CSV 다운로드" 버튼 제공
- 7개국 × 3개월 데모 데이터 포함

**완료 기준:**
- [ ] 드래그앤드롭 CSV 업로드 작동
- [ ] 미리보기 테이블 표시
- [ ] DB 저장 성공
- [ ] 에러 케이스 처리
- [ ] 샘플 CSV 다운로드

---

## Phase 4: AI 주간 브리핑 (Day 5~6)

### 4-1. 브리핑 생성 API (SSE 스트리밍)

**API Route: `/api/briefing/generate/route.ts`**

```
플로우:
1. 클라이언트에서 기간 (start_date, end_date) + user_id 전달
2. 서버에서:
   a. 해당 기간 매출 데이터 조회 (monthly_sales)
   b. 환율 데이터 조회 (exchange_rates)
   c. 경쟁사 메모 조회 (competitor_notes)
   d. 전월 데이터 조회 (비교용)
   e. 데이터를 프롬프트에 조합
   f. Claude API 호출 (SSE 스트리밍)
   g. 클라이언트에 실시간 전달
3. 생성 완료 시 briefings 테이블에 저장
```

**Claude API 호출 설정:**
- 모델: claude-3-haiku (빠르고 저렴)
- max_tokens: 2000
- temperature: 0.3 (일관된 분석)
- 스트리밍: true

**응답 형식:**
- JSON 파싱하여 6장 카드 데이터로 변환
- 파싱 실패 시 전체 텍스트를 1장 카드로 표시

**완료 기준:**
- [ ] API Route에서 Claude API 호출 성공
- [ ] SSE 스트리밍으로 실시간 전달
- [ ] 생성 완료 시 DB 저장
- [ ] 에러 처리 (API 키 없음, 데이터 없음 등)

---

### 4-2. 브리핑 UI

**생성 화면:**
- 기간 선택: "이번 주" / "이번 달" / "직접 선택"
- "AI 브리핑 생성" 버튼 (큰 버튼, 강조 색상)
- 생성 중: 스켈레톤 카드 + 타이핑 효과 텍스트 표시
- 데이터 부족 시: "매출 데이터를 먼저 입력해주세요" 안내

**결과 화면: 6장 카드 캐러셀** (사장님비서 브리핑과 동일 패턴)

```
카드 1: 📊 매출 종합
 - 총 매출 (원화), 전기간 대비 증감, 핵심 한 줄 요약

카드 2: 🌍 국가별 분석
 - 국가별 매출 증감, 최고/최저 국가, 원인 분석

카드 3: 💱 환율 영향
 - 주요 통화 환율 변동, 원화 환산 영향 분석

카드 4: ⚠️ 주요 이슈
 - 리스크 요인, 주목해야 할 변화

카드 5: 🏢 경쟁사 동향
 - 입력된 경쟁사 메모 기반 요약 (없으면 스킵)

카드 6: ✅ 액션 제안
 - 다음 주/달에 실행할 것 3~4개 체크리스트
```

- 좌우 화살표 또는 스와이프로 카드 이동
- 각 카드 하단: "이미지로 저장" 버튼 (선택)
- 전체: "PDF 다운로드" 버튼

**완료 기준:**
- [ ] AI 브리핑 생성 버튼 → SSE 스트리밍 → 카드 표시 전체 플로우 작동
- [ ] 6장 카드 캐러셀 정상 작동
- [ ] 타이핑 효과 자연스러움
- [ ] 생성된 브리핑 DB 저장 + 아카이브에서 조회 가능

---

### 4-3. 브리핑 아카이브

**기능:**
- 과거 브리핑 목록 (날짜순)
- 각 항목 클릭 시 해당 브리핑 카드 다시 보기
- 삭제 가능

**완료 기준:**
- [ ] 과거 브리핑 목록 표시
- [ ] 클릭 시 상세 보기
- [ ] 삭제 작동

---

## Phase 5: 환율 + 경쟁사 + 홈 (Day 7~8)

### 5-1. 환율 현황

**기능:**
- 등록된 국가의 통화별 현재 환율 표시
- 전일 대비 등락 (▲/▼ + %)
- 30일 추이 라인 차트
- 간단 환산 계산기: 금액 입력 → 원화 변환

**환율 데이터:**
- 무료 API 사용 (exchangerate-api.com 또는 한국수출입은행)
- 하루 1회 갱신 → DB에 캐싱
- API 호출 실패 시 최근 캐시 데이터 사용

**완료 기준:**
- [ ] 통화별 환율 테이블 표시
- [ ] 등락 표시 작동
- [ ] 30일 추이 차트 렌더링
- [ ] 환산 계산기 작동

---

### 5-2. 경쟁사 메모

**기능:**
- 경쟁사 이름 + 메모 내용 + 출처 URL + 날짜 입력
- 목록 표시 (최신순)
- AI 브리핑 생성 시 이 메모가 자동으로 포함됨

**UI:**
- 심플한 카드형 메모
- 입력 폼은 상단에 항상 표시
- 타임라인 형식 목록

**완료 기준:**
- [ ] 경쟁사 메모 CRUD 작동
- [ ] AI 브리핑에 메모 데이터 포함 확인

---

### 5-3. 홈 대시보드

**구성:**

```
상단: 오늘의 핵심 지표
├── 이번 달 총 매출 (원화)
├── 전월 대비 증감 (%)
├── 가장 성장한 국가
└── 최근 환율 변동 요약

중단: 국가별 매출 미니 차트
├── 7개국 가로 바 차트 (이번 달 vs 저번 달)
└── 클릭 시 /sales로 이동

하단: 최근 AI 브리핑 미리보기
├── 가장 최근 브리핑의 "매출 종합" 카드
├── "전체 보기" 링크 → /briefing
└── 브리핑 없으면 "첫 AI 브리핑을 생성해보세요" CTA
```

**완료 기준:**
- [ ] 핵심 지표 4개 카드 표시
- [ ] 국가별 미니 차트 표시
- [ ] 최근 브리핑 미리보기 표시
- [ ] 데이터 없을 때 적절한 Empty State

---

## Phase 6: 랜딩 + 마무리 (Day 9~10)

### 6-1. 랜딩 페이지

**구성:**
```
Hero: "매일 아침 2시간 걸리던 해외 실적 보고를 5분으로."
     → 시작하기 버튼

섹션 1: 문제 제기
 - 7개국 매출 수동 취합... 환율 직접 계산... 주간 보고서 수동 작성...

섹션 2: 핵심 기능 3개
 - 📊 국가별 매출 대시보드
 - 🤖 AI 주간 경영 브리핑
 - 💱 실시간 환율 연동

섹션 3: 대시보드 스크린샷 (실제 화면 캡처)

Footer: "글로벌 마켓 브리핑 — 해외 법인 경영 데이터를 AI로 자동화"
       기술 스택 뱃지: Next.js / TypeScript / Supabase / Claude AI / Vercel
```

**완료 기준:**
- [ ] 랜딩 페이지 완성
- [ ] 반응형 기본 대응
- [ ] 시작하기 → 로그인/회원가입 연결

---

### 6-2. 데모 데이터 시딩

**온보딩 플로우:**
1. 회원가입 완료 후 → 온보딩 페이지
2. "데모 데이터로 시작하기" / "직접 시작하기" 선택
3. 데모 선택 시:
   - 7개국 자동 생성
   - 12개월 매출 데이터 자동 생성 (현실적 트렌드 포함)
   - 환율 데이터 시딩
   - 경쟁사 메모 3개 시딩
4. 홈 대시보드로 이동 (데이터가 채워진 상태)

**완료 기준:**
- [ ] 데모 데이터 시딩 작동
- [ ] 데모 후 모든 페이지에서 데이터 표시 확인

---

### 6-3. 배포 + README

**Vercel 배포:**
- GitHub 연동
- 환경변수 설정
- 도메인: global-market-briefing.vercel.app

**README.md 작성:**
- 프로젝트 소개 (포트폴리오 목적 명시)
- 핵심 기능 설명 + 스크린샷
- 기술 스택
- 아키텍처 설명 (데이터 흐름도)
- 로컬 실행 방법
- 시작하기 안내

**완료 기준:**
- [ ] Vercel 배포 완료
- [ ] README.md 작성 완료
- [ ] 라이브 URL 정상 작동

---

## 체크포인트 요약

| Day | 완료 상태 | 핵심 결과물 |
|-----|----------|-----------|
| Day 1 | ⬜ | 프로젝트 셋업 + Supabase + 폴더 구조 |
| Day 2 | ⬜ | 인증 + 대시보드 레이아웃 |
| Day 3 | ⬜ | 국가 설정 + 매출 수동 입력 |
| Day 4 | ⬜ | 매출 대시보드 (차트+테이블) + CSV 업로드 |
| Day 5 | ⬜ | AI 브리핑 API (SSE) + Claude 연동 |
| Day 6 | ⬜ | 브리핑 UI (6장 캐러셀) + 아카이브 |
| Day 7 | ⬜ | 환율 현황 + 경쟁사 메모 |
| Day 8 | ⬜ | 홈 대시보드 + 전체 연결 |
| Day 9 | ⬜ | 랜딩 페이지 + 데모 데이터 |
| Day 10 | ⬜ | 배포 + README + 최종 점검 |

---

## 면접 대응: 이 프로젝트로 설명할 수 있는 것

| 면접 질문 | 이 프로젝트에서의 답변 |
|----------|---------------------|
| "왜 이 프로젝트를 만들었나요?" | "글로벌 제조업체 경영지원팀의 해외 시장 분석·보고 업무를 이해하고, AI 자동화 가능성을 직접 검증해봤습니다" |
| "기술 스택을 왜 이렇게 선택했나요?" | "Next.js는 풀스택 가능, Supabase는 무료+RLS 보안, Claude는 분석 품질이 높아서" |
| "데이터 흐름을 설명해주세요" | "CSV 업로드 → 파싱 → DB 저장 → 대시보드 조회 → AI가 데이터 읽어서 브리핑 생성" |
| "AI를 어떻게 활용했나요?" | "매출+환율+경쟁사 데이터를 프롬프트에 조합하고, Claude가 경영 분석 브리핑을 생성합니다" |
| "실무와 다른 점은 뭘까요?" | "실무에서는 ERP/SAP 연동, 실시간 데이터, 권한 관리 등이 추가되겠지만, 자동화 흐름의 핵심 구조는 동일합니다" |
