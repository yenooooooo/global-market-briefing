# ── 의존성 ──
/node_modules

# ── Next.js 빌드 출력 ──
/.next/
/out/

# ── 프로덕션 빌드 ──
/build

# ── 환경 변수 (민감 정보 — 절대 커밋 금지) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── 디버그 로그 ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── IDE/에디터 ──
.vscode/
.idea/
*.swp
*.swo

# ── OS 파일 ──
.DS_Store
Thumbs.db

# ── TypeScript 빌드 정보 ──
*.tsbuildinfo
next-env.d.ts

# ── Vercel ──
.vercel

# ── 참고 디자인 파일 ──
stitch*/

# ── 임시/이전 이미지 ──
public/images/*-old.*
public/images/매출*
